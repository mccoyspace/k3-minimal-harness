#!/usr/bin/env bash
# Backward-compatible name for selecting the K3 studio profile.
set -euo pipefail

project_dir=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd -P)
exec "$project_dir/bin/restart-waste-profile" k3
