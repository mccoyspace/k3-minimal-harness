# Project state

Goal: make Kimi K3 useful through the smallest practical local-agent loop.

The harness supports approved batched shell inspection, direct terminal file
saves, compact JSONL transcripts, and exact WASTE conversation-head reuse.
`SAVE relative/path` content is written by the host rather than embedded in a
JSON shell command. Responses stopped by the output-token limit are never
executed or saved.

Replace this paragraph with a short description of the current project,
important decisions, and the next useful step. Keep this file under 200 words;
it is the durable cold-start note when a session or WASTE process restarts.
